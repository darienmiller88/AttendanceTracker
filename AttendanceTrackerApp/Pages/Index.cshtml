@page

@* connects this page to the IndexModel class *@
@model IndexModel

@section Styles{
    <link rel="stylesheet" href="~/css/index.css"/>
}

<div>
    @* Allows the html file to access the fields of the IndexModel class *@
    @Model.Message
</div>

<h1>
    I might be redeemable actually if a miracle happens
</h1>